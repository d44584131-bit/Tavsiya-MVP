name: tavsiya
description: Tavsiya — справочник мест с отзывами (Ташкент)
publish_to: 'none'
version: 0.1.1+7

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.5.6
  shared_preferences: ^2.3.2
  http: ^1.2.2
  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  google_fonts: ^6.2.1
  share_plus: ^13.3.0
  url_launcher: ^6.3.2
  # для будущей локализации интерфейса (RU/UZ):
  # flutter_localizations:
  #   sdk: flutter
  # intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
